#include "json_parser.hpp"
#include <json/json.h>

std::vector<std::string> JsonParser::parseEntities(const std::string& json)
{
    // Parse JSON and extract entity names
    return std::vector<std::string>{};
}
